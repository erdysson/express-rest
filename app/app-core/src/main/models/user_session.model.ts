import {model, Model, Schema} from 'mongoose';import {IUserSessionModel} from '../interfaces/models.interface';const UserSessionSchema: Schema = new Schema({    id: {        type: Schema.Types.ObjectId,        unique: true,        auto: true    },    token: {        type: Schema.Types.String,        required: true    },    created: {        type: Schema.Types.Date,        default: Date.now    },    updated: {        type: Schema.Types.Date    }});const UserSessionModel: Model<IUserSessionModel> = model<IUserSessionModel>('UserSession', UserSessionSchema);export default UserSessionModel;