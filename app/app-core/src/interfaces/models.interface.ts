import {Document} from 'mongoose';export interface IUserModel extends Document {    email: string;    password: string;    name: string;    lastName: string;    created: Date;    updated: Date;}